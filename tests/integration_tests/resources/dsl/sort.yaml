tosca_definitions_version: cloudify_dsl_1_4

imports:
  - cloudify/types/types.yaml

node_templates:
  node:
    type: cloudify.nodes.Root
    instances:
      deploy: 2
  node2:
    type: cloudify.nodes.Root
    instances:
      deploy: 2
  node3:
      type: cloudify.nodes.Root
      instances:
        deploy: 2
