tosca_definitions_version: cloudify_dsl_1_3

imports:
  - file:///opt/manager/resources/spec/cloudify/5.0.dev1/types.yaml

node_templates:

  deployment_proxy:
    type: cloudify.nodes.DeploymentProxy
    properties:
      resource_config:
        blueprint:
          external_resource: true
          id: basic
        deployment:
          id: component
