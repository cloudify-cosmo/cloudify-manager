imports:
    - cloudify.types
    - testmockoperations-plugin.yaml
    - mock-workflows-plugin.yaml
    - policies/threshold.yaml
    - triggers/execute_workflow.yaml

node_templates:
    node:
        type: cloudify.types.base
        interfaces:
            test:
                - op1: testmockoperations-plugin.testmockoperations.tasks.mock_operation_from_custom_workflow

groups:
    threshold_group:
        members: [node]
        policies:
            threshold_policy:
                type: threshold
                properties:
                    metric: TODO
                    threshold: 60
                triggers:
                    threshold_exceeded_workflow:
                        type: execute_workflow
                        parameters:
                            workflow: threshold_exceeded

workflows:
    threshold_exceeded: mock-workflows-plugin.mock_workflows.workflows.test_policies_1
