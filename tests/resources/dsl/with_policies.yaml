imports:
    - cloudify.types
    - policies/threshold.yaml

node_templates:
    node:
        type: cloudify.types.base

groups:
    threshold_group:
        members: [node]
        policies:
            threshold_policy:
                type: threshold
                properties:
                    metric: TODO
                    threshold: 60
