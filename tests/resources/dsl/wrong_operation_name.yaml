imports:
    - "cloudify.types"
    - testmockoperations.yaml

blueprint:
    name: "mock_app"
    nodes:
        -   name: "mock_node"
            type: "mock_type"

types:
    mock_type:
        derived_from: cloudify.types.base
        interfaces:
            cloudify.interfaces.lifecycle:
                - start: testmockoperations.non_existent
