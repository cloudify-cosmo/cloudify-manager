tosca_definitions_version: 'cloudify_dsl_1_2'

imports:
  - cloudify/types/types.yaml

description: >
  Micro web sites hosting.

inputs:
  ip_input:
    type: string
    default: ''

node_templates:
  site1:
    type: cloudify.nodes.Compute
    properties:
      prop1: { get_input: ip_input }
