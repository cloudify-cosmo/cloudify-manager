imports:
    - cloudify.types
    - testmockoperations.yaml

node_templates:
    test_node:
        type: cloudify.types.host
        properties:
            install_agent: false
        interfaces:
            cloudify.interfaces.host:
                - get_state:
                    mapping: testmockoperations.tasks.host_get_state
                    properties:
                        false_count: 3
