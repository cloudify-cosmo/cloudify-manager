imports:
    - cloudify.types
    - testmockoperations.yaml

blueprint:
    name: test_workflow_retries_4
    nodes:
        -   name: test_node
            type: cloudify.types.base
            interfaces:
                cloudify.interfaces.lifecycle:
                    - create:
                        mapping: testmockoperations.tasks.fail
                        properties:
                            fail_count: 100000
                            non_recoverable: true
