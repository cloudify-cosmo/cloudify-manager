imports:
  - cloudify.types
  - cloudmock.yaml

blueprint:
    name: multi_instance_many_different_hosts
    nodes:
        -   name: host1
            type: cloudify.types.base
            instances:
                deploy: 100
