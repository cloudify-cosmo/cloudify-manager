language: python
python:
  - "2.7"

install:

    # command to install dependencies
    - python setup.py install

# command to run tests
script:
    - 'python -m unittest plugin_installer.tests.test_installer'