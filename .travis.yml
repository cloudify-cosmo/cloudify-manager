language: python
python:
    - "2.7"

services:
    - rabbitmq

env:
    - ACTION="test-plugins"
    - ACTION="test-rest-service"
    - ACTION="run-integration-tests"
    - ACTION="flake8"

script:
    - ./run-tests.sh $ACTION
