interfaces:
    mock_interface:
        operations:
            - "make_reachable"

plugins:
    testmockoperations:
        derived_from: "cloudify.tosca.artifacts.agent_plugin"
        properties:
            interface: "mock_interface"
            url: "mock_interface.zip"
