###
# connection configurer mock plugin definition.
plugins:
    connection_configurer_mock:
        derived_from: "cloudify.tosca.artifacts.remote_plugin"
        properties:
            interface: "cloudify.tosca.interfaces.connection_configurer"
            url: "connection_configurer_mock.zip"
