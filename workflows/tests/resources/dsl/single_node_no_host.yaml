imports:
    - "cloudify.types"
    - "testmockoperations.yaml"

blueprint:
    name: "single_host_nost"
    topology:
        -   name: "single_node"
            type: "mock_type"


types:
    mock_type:
        derived_from: cloudify.types.base
        interfaces:
            -   "mock_interface"
        workflows:
            create:
                radial: |
                    define mock_create
                        execute_operation operation: 'make_reachable'
            delete:
                radial: |
                    define mock_delete
                        execute_operation operation: 'make_unreachable'
