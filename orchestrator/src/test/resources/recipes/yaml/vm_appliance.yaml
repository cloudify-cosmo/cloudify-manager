vm_appliance:
  type: appliance
  workflows:
    - start_appliance
    - monitor
    - unmonitor
    - run_script_on_vm
  resources:
    - cosmo_agent
    - tomcat

tomcat:
  type: resource
  config:
    default_configurer: chef
    chef_recipe: tomcat.rb

cosmo_agent:
  type: resource