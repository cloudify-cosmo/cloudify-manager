define start_node
  execute_task target: "$resource_provisioner", continue_on: "started", timeout: 20s, payload: {
    exec: "start_machine",
    resource_id: "$resource.id"
  }
  resource_monitor resource_id: "$resource_id"
  state resource_id: "$resource_id", reachable: "true"



