imports:
    - "cloudify.types"
    - "host_provisioner"

blueprint:
    name: "some_service_template"
    topology:
        -   name: "some_host"
            type: "cloudify.types.host"

workflows:
    install:
        ref: "not-found"

