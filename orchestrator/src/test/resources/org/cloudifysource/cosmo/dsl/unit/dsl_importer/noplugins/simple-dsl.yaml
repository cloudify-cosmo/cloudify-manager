definitions:

    imports:
        - "cloudify.types"
        - "host.provisioner"

    service_templates:
        app:
            topology:
                host:
                    type: "cloudify.tosca.types.host"