definitions:

    imports:
        - "cloudify.types"
        - "host_provisioner"

    service_templates:
        app:
            topology:
                host:
                    type: "cloudify.tosca.types.host"