definitions:

    imports:
        - "cloudify.types"
        - "host.provisioner"

    service_templates:
        some_template:
            topology:
                some_host:
                    type: "cloudify.tosca.types.host"

