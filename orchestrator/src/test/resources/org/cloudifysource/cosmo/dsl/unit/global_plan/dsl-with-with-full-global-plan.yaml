definitions:

    global_plan: "full_global_workflow.radial"

    imports:
        - "cloudify.types"
        - "host.provisioner"

    service_templates:
        some_template:
            topology:
                some_host:
                    type: "cloudify.tosca.types.host"

