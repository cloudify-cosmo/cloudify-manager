global_plan: "full_global_workflow.radial"

imports:
    - "cloudify.types"
    - "host_provisioner"

application_template:
    name: "some_template"
    topology:
        -   name: "some_host"
            type: "cloudify.tosca.types.host"

