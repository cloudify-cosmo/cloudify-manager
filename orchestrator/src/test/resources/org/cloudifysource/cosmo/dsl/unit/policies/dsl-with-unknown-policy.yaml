imports:
    - "cloudify.types"
    - "host_provisioner"
    - "middleware_component_installer"

application_template:
    name: "web_server"
    topology:
        -   name: "webserver_host"
            type: "cloudify.tosca.types.host"
            policies:
                unknown_policy:
                    rules:
                        -   type: "state_equals"
                            properties:
                                state: "host_state"
                                value: "running"
