
imports:
    - "cloudify.types"

plugins:
    openstack_host_provisioner:
        derived_from: "cloudify.plugins.manager_plugin"
        properties:
            url: "https://github.com/CloudifySource/cosmo-plugin-openstack-provisioner/archive/develop.zip"
