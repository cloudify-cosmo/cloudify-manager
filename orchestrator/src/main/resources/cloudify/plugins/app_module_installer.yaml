
imports:
    - "cloudify.types"

plugins:
    cloudify.plugins.app_module_installer:
        derived_from: "cloudify.plugins.agent_plugin"
        properties:
            interface: "cloudify.interfaces.app_module_installer"
            url: "cloudify/plugins/app_module-installer.zip"
