
imports:
    - "cloudify.types"

plugins:
    cloudify.plugins.vagrant_host_provisioner:
        derived_from: "cloudify.plugins.remote_plugin"
        properties:
            url: "cloudify/plugins/vagrant-host-plugin.zip"
