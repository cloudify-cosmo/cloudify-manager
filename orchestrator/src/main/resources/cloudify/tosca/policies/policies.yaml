definitions:

    ##################################################################################
    # Policies & rules definitions
    ##################################################################################
    policies:
        types:
            start_detection_policy: |
                abcdefg...

            failure_detection_policy: |
                12345676...

        rules:
            state_equals: |
                (service "${service}")
                (status "${state}")

            state_not_equals: |
                 (service "${service}")
                 (not (status "${state}"))

            metric_below: |
                 (service "${service}")
                 (< (metric "${metric}"))
