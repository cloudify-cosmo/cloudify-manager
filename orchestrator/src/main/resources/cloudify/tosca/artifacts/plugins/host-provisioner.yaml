definitions:

    # TODO DSL we should probably add an explicit import here to the base types

    artifacts:
        cloudify.tosca.artifacts.plugin.host.provisioner:
            derived_from: "cloudify.tosca.artifacts.plugin"
            properties:
                interface: "cloudify.tosca.interfaces.host.provisioner"
                url: "cloudify/tosca/artifacts/plugins/host-provisioner.zip"
