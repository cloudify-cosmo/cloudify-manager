definitions:

    ##################################################################################
    # Policies rules definitions
    ##################################################################################
    policies:
        rules:
            status_equal: |
                (rule stub)

            status_not_equal: |
                (rule stub)

            metric_greater_than: |
                (rule stub)
