definitions:

    ##################################################################################
    # Policies rules definitions
    ##################################################################################
    policies:
        types:
            start_detection_policy: { }
            failure_detection_policy: { }

        rules:
            status_equal:

            status_not_equal:

            metric_greater_than:

