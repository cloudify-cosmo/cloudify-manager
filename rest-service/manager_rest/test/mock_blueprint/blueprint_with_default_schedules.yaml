tosca_definitions_version: cloudify_dsl_1_3

imports:
  - cloudify/types/types.yaml

deployment_settings:
  default_schedules:
    sc1:
      workflow: install
      since: '+1w'
      until: '+1y'
      recurring: 1w
      weekdays: [mo, fr]
      count: 5
    sc2:
      workflow: install
      since: '2022-1-1 15:00'
      until: '+1d'
      timezone: 'Israel'
      recurring: 1mo
      weekdays: [ l-fr ]
      count: 10
