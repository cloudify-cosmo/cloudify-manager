import org.cloudifysource.cosmo.monitor.mock.MonitoringMessage

rule "Change message type from before to after"

  dialect "mvel"

  when
      message : MonitoringMessage( type == "before", msgtext : msgtext )
  then
      System.out.println( msgtext );
      modify ( message ) {
        type = "after"
      }
end