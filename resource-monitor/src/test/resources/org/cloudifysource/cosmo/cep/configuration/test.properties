# suppress inspection "UnusedProperty" for whole file

resource.monitor.port=8080

# agent hint
agent.id=agent_1

# resource monitor server configuration
rule.file=/org/cloudifysource/cosmo/cep/AgentFailureDetector.drl
pseudo.clock=false
input.uri=http://localhost:${resource.monitor.port}/input/
output.uri=http://localhost:${resource.monitor.port}/output/
