import org.cloudifysource.cosmo.cep.Message

rule "Change message type from before to after"

  dialect "mvel"

  when
      message : Message( type == "before", msgtext : msgtext )
  then
      System.out.println( msgtext );
      modify ( message ) {
        type = "after"
      }
end