import org.cloudifysource.cosmo.resource.monitor.Message

rule "Hello World"
      dialect "mvel"
  when
      m : Message( type == "before", msgtext : msgtext )
  then
      System.out.println( msgtext );
      modify ( m ) {
        type = "after"
      }
end