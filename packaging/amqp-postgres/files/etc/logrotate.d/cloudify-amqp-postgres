/var/log/cloudify/amqp-postgres/*.log {
        hourly
        rotate 7
        size 100M
        copytruncate
        compress
        delaycompress
        missingok
        notifempty
        nodateext
}
