
imports:
    - "cloudify.types"

plugins:
    cloudify.plugins.middleware_component_installer:
        derived_from: "cloudify.plugins.agent_plugin"
        properties:
            url: "cloudify/plugins/middleware_component-installer.zip"
